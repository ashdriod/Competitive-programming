#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include<limits.h>

int main() {

    int n,m;
    n=CHAR_MAX;
    m=CHAR_MIN;
    printf("Minimum value of signed character is %d\nMaximum value of signed character is %d",m,n);
    return 0;
}
